#!/usr/bin/bash

system_loc=~/Personal

if [ $# = 0 ]; then
    find ${system_loc}/References/Books -type f | cut -sd / -f 7-
else
    book=$(echo "${1}" | rev | cut -d / -f 1 | rev)
    i3-msg exec xdg-open $(find ${system_loc}/References/Books -type f -name "${book}") > /dev/null
fi
