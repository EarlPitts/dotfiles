#!/usr/bin/bash

system_loc=~/Personal

if [ $# = 0 ]; then
    find ${system_loc}/References/Cheatsheets -type f -printf "%f\n"
else
    i3-msg exec xdg-open $(find ${system_loc}/References/Cheatsheets -type f -name "$1") > /dev/null
fi
