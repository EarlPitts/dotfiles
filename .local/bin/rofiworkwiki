#!/usr/bin/bash

system_loc=~/Work

if [ $# = 0 ]; then
    find $system_loc/Notes -type f -name '*.md' -printf "%f\n"
else
    i3-msg exec "i3-sensible-terminal -e zsh -ic 'cd ${system_loc}/Notes && nvim ${1}'" > /dev/null
fi
