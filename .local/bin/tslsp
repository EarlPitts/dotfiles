if [ -f $HOME/.nvm/nvm.sh ]; then
  type nvm >/dev/null 2>&1 || . $HOME/.nvm/nvm.sh
  nvm use $(< .nvmrc) || nvm install && nvm use $(< .nvmrc) 
fi

export NODE=$(dirname "$(dirname "$(nvm which | tail -n1)")")
