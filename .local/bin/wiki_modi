#!/usr/bin/bash

if [ $# = 0 ]; then
    find ~/Personal/Notes -type f -name '*.md' -printf "%f\n"
else
    i3-msg exec "i3-sensible-terminal -e zsh -ic 'cd ~/Personal/Notes && nvim ${1}'" > /dev/null
fi
