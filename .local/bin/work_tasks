#!/usr/bin/bash

system_loc=~/Work

if [ "${1}" = open ]; then
    nvim '+lcd %:p:h' ${system_loc}/tasks.md
else
    task=$(cat ${system_loc}/tasks.md | fzf)
    [ -n "${task}" ] && sed -i "/^${task}$/d" ${system_loc}/tasks.md
fi
