#!/usr/bin/bash

system_loc=~/Work

if [ "${1}" = open ]; then
    nvim '+lcd %:p:h' ${system_loc}/waiting.md
else
    task=$(cat ${system_loc}/waiting.md | fzf)
    [ -n "${task}" ] && sed -i "/^${task}$/d" ${system_loc}/waiting.md
fi
